---
// Placeholder. This exists so the invariants have a page to run against on a
// fresh clone — it is not the prototype. Replace the whole thing once the
// explainer's one idea is decided; nothing here is worth keeping.
import { route } from "../lib/url";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Assignment 1 — interactive explainer</title>
  </head>
  <body>
    <nav aria-label="Main">
      <a href={route("")}>Home</a>
    </nav>
    <main>
      <h1>Nothing here yet</h1>
      <p>
        The explainer goes here: one idea, one mechanic, and nothing else.
      </p>
    </main>
  </body>
</html>
